<% provide(:title, "Events") %>
<%= button_to "Search", search_events_path, method: :get%>
<% if logged_in? %>
  <% if is_organizer? %>
    <%= button_to "New Event", search_available_rooms_path, method: :get %>
  <% end %>
<% end %>
<%= render "index" %>